# Usado pelo docker-compose (copie para .env na mesma pasta)
# Variáveis da raiz do projeto podem ser reutilizadas aqui

AIRFLOW_UID=50000

# AWS (opcional; ou use volume ~/.aws no container)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
